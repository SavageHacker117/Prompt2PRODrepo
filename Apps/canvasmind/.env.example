# ===== CanvasMind root env sample =====
# Copy to .env (root) or to app/server subfolders as needed.

# --- Web demo (apps/demo-web) ---
VITE_API_BASE=/api

# --- Gateway (server/gateway) ---
GATEWAY_HOST=0.0.0.0
GATEWAY_PORT=8080
CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Registry seed (relative to repo root)
MCP_REGISTRY_FILE=server/registry/seeds/mcp.registry.json

# --- Telemetry (server/telemetry) ---
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_DB=canvasmind
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
