# Pyro (Vite + React + Three.js)

Run:
1) npm i
2) npm run dev
Open http://localhost:5173

Click + drag the fireball to set power and direction. Light all torches with limited shots.
Levels live in `src/levels/`. Add more as needed.
