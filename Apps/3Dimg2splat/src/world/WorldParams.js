// src/world/WorldParams.js
export const DefaultWorldParams = {
  seaLevel: 10,      // blocks; raise/lower the ocean
  heightOffset: 10,  // add to raw height; shift terrain up/down
  chunkSize: 32,
  maxHeight: 64,
};
